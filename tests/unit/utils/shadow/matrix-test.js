import shadowMatrix from '../../../utils/shadow/matrix';
import { module, test } from 'qunit';

module('Unit | Utility | shadow/matrix');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shadowMatrix();
  assert.ok(result);
});
