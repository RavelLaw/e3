import shadowEvent from '../../../utils/shadow/event';
import { module, test } from 'qunit';

module('Unit | Utility | shadow/event');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shadowEvent();
  assert.ok(result);
});
