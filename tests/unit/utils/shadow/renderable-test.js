import shadowRenderable from '../../../utils/shadow/renderable';
import { module, test } from 'qunit';

module('Unit | Utility | shadow/renderable');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shadowRenderable();
  assert.ok(result);
});
