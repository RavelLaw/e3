import e3Interpolate from '../../../utils/e3-interpolate';
import { module, test } from 'qunit';

module('Unit | Utility | e3 interpolate');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = e3Interpolate();
  assert.ok(result);
});
