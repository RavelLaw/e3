import { e3ScaleMin } from '../../../helpers/e3-scale-min';
import { module, test } from 'qunit';

module('Unit | Helper | e3 scale min');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = e3ScaleMin(42);
  assert.ok(result);
});
