import { e3ScaleMax } from '../../../helpers/e3-scale-max';
import { module, test } from 'qunit';

module('Unit | Helper | e3 scale max');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = e3ScaleMax(42);
  assert.ok(result);
});
