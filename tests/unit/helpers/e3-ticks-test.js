import { e3Ticks } from 'ember-e3/helpers/e3-ticks';
import { module, test } from 'qunit';

module('Unit | Helper | e3 ticks');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = e3Ticks([], {});
  assert.ok(result);
});
